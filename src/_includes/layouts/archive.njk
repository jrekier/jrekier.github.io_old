---
layout: layouts/base.njk
pagination:
  data: collections
  size: 1
  alias: tag
permalink: /tags/{{ tag }}/
featured_image: /images/banners/archive-bg.jpg
---

<header class="pb-24">
  {% include "navbar.njk" %}
</header>

{% include "banner.njk" %}

<div class="container max-w-4xl mx-auto md:flex items-start py-20 px-12 md:px-0">

  <!-- articles -->
  <div class="w-full md:pr-12 mb-12 px-5">
    {# <div class="markdown">
      {{ content | safe }}
    </div> #}
    {% set taglist = collections[ tag ] %}
    {%- for post in taglist | reverse -%}
      <article class="mb-8">
        <a href="{{ post.url }}">    
          {# <div class="inline-block w-full mb-4 pb-2 text-black rounded-lg group hover:bg-gray-100"> #}
          <div class="flex items-center w-full mb-4 pb-2 text-black rounded-lg group">
            {# <!-- Image -->
            {% if post.data.featured_image %}
            <div class="rounded-t-lg h-48 overflow-hidden">
              <img src="{{ post.data.featured_image }}" class="h-48 w-full object-cover group-hover:opacity-75">
            </div>
            {% endif %} #}
            {% if post.data.cover_image %}
              <img class="inline-block w-16 mx-auto mr-4 " src="{{ post.data.cover_image }}"/>
            {% endif %}
            <div class="inline-block">
              <!-- Title -->
              <p class="px-1 pt-2 font-serif group-hover:text-red-700 text-xl md:text-2xl uppercase">
                {{ post.data.title }}
              </p>
              <!-- description -->
              <p class="px-1 text-gray-700 leading-normal mb-2">
                {{post.data.description}}
              </p>
              <div class="px-1 mb-2 text-sm text-gray-700">
                by <a href="#" class="text-gray-700 hover:text-red-700">{{post.data.author}}</a> on {{ post.date | readableDate }}
                <span class="font-bold mx-1"> | </span>
                <a href="#" class="text-gray-700 hover:text-red-700">{{post.data.category}}</a>
                <span class="font-bold mx-1"> | </span>
                {% for tag in post.data.tags %}
                <a href="/tags/{{tag}}"><span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-xs font-semibold text-gray-700 hover:text-red-400 mr-2">#{{tag}}</span></a>
                {% endfor %}
              </div>
            </div>
          </div>
        </a>
      </article>
    {%- endfor -%}

  </div>

</div>